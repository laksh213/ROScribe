"""ROS Metadata Extractor V2 — desktop registry app.

Reuses the proven extraction engine (metadata_extractor.extractor) and adds a
7-column registry format, per-row document attach/open, username logging, and a
GitHub-based update check.
"""
